import { extendTheme } from '@chakra-ui/react';

const merchantTheme = extendTheme({
  colors: {
    brand: {
      primary: '#003f5c',
      secondary: '#007aff',
    }
  }
});

export default merchantTheme;
